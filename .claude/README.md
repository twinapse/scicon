# Claude Code local setup

This directory is reserved for Claude Code project settings.

Keep personal, machine-specific settings in `.claude/settings.local.json`.
That file is gitignored.
If shared team settings are needed later, add `.claude/settings.json` and document the behavior in [docs/coding-agent-setup.md](../docs/coding-agent-setup.md).
